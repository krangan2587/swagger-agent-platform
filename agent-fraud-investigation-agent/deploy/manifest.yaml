# Generated by the Output Packager (Stage 5) from deployment.* (Section 4.12).
# DO NOT EDIT BY HAND.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fraud-investigation-agent
  labels:
    agent: fraud-investigation-agent
    version: "2.3.1"
spec:
  strategy:
    type: canary
  template:
    metadata:
      labels:
        agent: fraud-investigation-agent
    spec:
      containers:
        - name: fraud-investigation-agent
          image: "fraud-investigation-agent:2.3.1"
          env:
            - name: AGENT_NAME
              value: "fraud-investigation-agent"
            - name: AGENT_VERSION
              value: "2.3.1"
---
# Target environments declared in the spec: ['dev', 'staging', 'production']
# Approval required before deploy: True
