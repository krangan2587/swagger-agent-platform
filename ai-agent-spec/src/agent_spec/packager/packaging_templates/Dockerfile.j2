# Generated by the Output Packager (Stage 5) from deployment.* (Section 4.12).
# DO NOT EDIT BY HAND.

FROM python:3.12-slim

WORKDIR /app
COPY src/ ./src/
COPY spec/ ./spec/

# TODO: add a requirements.txt / pyproject.toml install step once the
# generated tool bindings have real dependencies.

ENV AGENT_NAME="{{ ir.info.name }}"
ENV AGENT_VERSION="{{ ir.info.version }}"
ENV ROLLOUT_STRATEGY="{{ ir.deployment.rollout_strategy }}"

CMD ["python", "-m", "src.handlers"]
