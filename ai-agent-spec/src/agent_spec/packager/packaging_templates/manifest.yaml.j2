# Generated by the Output Packager (Stage 5) from deployment.* (Section 4.12).
# DO NOT EDIT BY HAND.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ ir.info.name }}
  labels:
    agent: {{ ir.info.name }}
    version: "{{ ir.info.version }}"
spec:
  strategy:
    type: {{ ir.deployment.rollout_strategy }}
  template:
    metadata:
      labels:
        agent: {{ ir.info.name }}
    spec:
      containers:
        - name: {{ ir.info.name }}
          image: "{{ ir.info.name }}:{{ ir.info.version }}"
          env:
            - name: AGENT_NAME
              value: "{{ ir.info.name }}"
            - name: AGENT_VERSION
              value: "{{ ir.info.version }}"
---
# Target environments declared in the spec: {{ ir.deployment.environments }}
# Approval required before deploy: {{ ir.deployment.approval_required }}
